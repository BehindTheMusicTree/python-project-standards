# Tier A (and optional Tier B): shared pre-commit in CI — delegate to org reusable (pin @v… to STANDARDS_VERSION).
name: Lint

on:
  pull_request:
  push:
    branches: [main]

jobs:
  lint:
    uses: BehindTheMusicTree/python-project-standards/.github/workflows/reusable-pre-commit.yml@v4.1.0
