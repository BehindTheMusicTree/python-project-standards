# Tier A (and optional Tier B): shared lint — delegate to org reusable (pin @v… to STANDARDS_VERSION).
name: Lint

on:
  pull_request:
  push:
    branches: [main]

jobs:
  lint:
    uses: BehindTheMusicTree/python-project-standards/.github/workflows/reusable-lint.yml@v1.0.0
