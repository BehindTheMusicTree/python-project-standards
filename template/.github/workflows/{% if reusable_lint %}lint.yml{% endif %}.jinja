# Shared pre-commit in CI — delegates to the org reusable workflow; `copier update` bumps the pin.
name: Lint

on:
  pull_request:
  push:
    branches: [main]

jobs:
  lint:
    uses: BehindTheMusicTree/python-project-standards/.github/workflows/reusable-pre-commit.yml@v4.3.2
