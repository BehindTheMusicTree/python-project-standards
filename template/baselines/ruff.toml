# Organization Ruff baseline (vendored copy per repo). Do not edit in consumer
# repositories except by upgrading python-project-standards templates.
# Optional per-repo ignores: use pyproject.toml [tool.ruff] overlay (see migration guide).

line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]
ignore = []
