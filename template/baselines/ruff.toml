# Organization Ruff baseline, managed by Copier (see .copier-answers.yml).
# Local edits are kept and 3-way merged on `copier update`.
# Optional per-repo ignores: use pyproject.toml [tool.ruff] overlay (see migration guide).

line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]
ignore = []
